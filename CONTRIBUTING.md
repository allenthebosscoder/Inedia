# Contributing

Run the tracker tests with `python3 -m pytest` and the extension checks with `npm run typecheck && npm test` from `extension/`. Run `npm run build` before testing the unpacked extension. Never include personal data, resumes, application history, browser profiles, secrets, or screenshots in commits.
